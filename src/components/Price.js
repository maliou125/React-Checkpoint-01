import React from 'react';
import product from "../product.js";

const Price = () => {
    return (
        <div>
           <h> {product.price} </h>
        </div>
    );
};

export default Price;
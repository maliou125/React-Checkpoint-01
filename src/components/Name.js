import React from 'react';
import product from "../product.js";

const Name = () => {
    return (
        <div>
           <h> {product.name} </h>
        </div>
    );
};

export default Name;